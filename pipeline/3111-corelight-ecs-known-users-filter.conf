# Corelight ECS Logstash Pipeline
# Git Repository: https://github.com/corelight/ecs-logstash-mappings
# Authors: Corelight Inc, Brasi Tech LLC
# License: BSD 3-Clause
# Support: https://github.com/corelight/ecs-logstash-mappings/issues/new
# Releases: https://github.com/corelight/ecs-logstash-mappings/releases

filter {
  if [@metadata][z_no_reuse][event_type] == "corelight" and [@metadata][z_no_reuse][event_sub_type] == "known-users" {

    mutate {
      update => {
        "[@metadata][temporary_metadata_index_name_suffix]" => "various"
        "[event][category]" => "observations"
      }
      rename => {
        "duration" => "[event][duration]"
        "host_ip" => "[host][ip]"
        "kuid" => "[log][id][kuid]"
        "last_active_interval" => "[corelight][known][last_active_interval]"
        "last_active_session" => "[log][id][last_active_session]"
        "num_conns" => "[corelight][known][num_conns]"
        "protocol" => "[network][protocol]"
        "remote_ip" => "[corelight][known][remote_ip]"
        "user" => "[user][name]"
      }
      copy => {
        "[log][id][kuid]" => "[event][id]"
      }
      add_field => {
        "[@metadata][etl][pipeline]" => "filter-mutate-a5166061923f-main-known-users-20220811.01"
      }
      tag_on_failure => "_mutate_error-a5166061923f"
      id => "filter-mutate-a5166061923f"
    }

    # Event duration
    if [event][duration] {
      ruby {
        code => '
          event_duration = event.get("[event][duration]")
          duration = event_duration.to_f * 1000000000
          event.set("[event][duration]", duration)
        '
        add_field => {
          "[@metadata][etl][pipeline]" => "filter-ruby-751c819809db-20220811.01"
        }
        tag_on_exception => "_rubyexception-751c819809db-event.duration_to_nanoseconds-20220811.01"
        id => "filter-ruby-751c819809db"
      }
    }
    
    # Remove blank/empty [log][id][last_active_session] field
    if [log][id][last_active_session] == "" {
      mutate {
        remove_field => [
          "[log][id][last_active_session]"
        ]
        add_field => {
          "[@metadata][etl][pipeline]" => "filter-mutate-f9cf362c6628-20220811.01"
        }
        tag_on_failure => "_mutate_error-f9cf362c6628"
        id => "filter-mutate-f9cf362c6628"
      }
    }

  }
}
